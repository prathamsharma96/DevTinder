# DevTinder Notes

## Running the Application

### Production Mode

```bash
npm start
```

Runs the server normally on port 3000.

### Development Mode

```bash
npm run dev
```

Runs the server with nodemon (auto-reloads when you save files).
